#ifndef DEFINES_H
#define DEFINES_H

#include <math.h>

#define FPS 60

#define GAME_WIDTH  320
#define GAME_HEIGHT 200
#define SCALE 3

#define MAX_ANGLE (M_PI * 2.0f)
#define DEG2RAD(a) ( ((a) * M_PI) / 180.0f )
#define RAD2DEG(a) ( ((a) * 180.0f) / M_PI )

#endif /* DEFINES_H */
